[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aws-quota-checker = { editable = true, extras = ["prometheus"], path = "." }

[dev-packages]
aws-quota-checker = { editable = true, extras = ["dev"], path = "." }

[requires]
python_version = "3.14"
