{{- range .Values.exporter.aws.regions }}
{{- $region := . }}
{{- with $ }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ printf "%s-%s" (include "aws-quota-checker.fullname" .) ($region) }}
  labels:
    {{- include "aws-quota-checker.commonLabels" . | nindent 4 }}
    aws-quota-checker.gravitational.io/region: {{ $region | quote }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "aws-quota-checker.selectorCommonLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "aws-quota-checker.commonLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        aws-quota-checker.gravitational.io/region: {{ $region | quote }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            {{- if .Values.exporter.enableDebugLogging }}
            - --debug
            {{- end }}
            - prometheus-exporter
            - --port
            - "8080"
            - --region
            - {{ $region | quote }}
            {{- if .Values.exporter.aws.profileName }}
            - --profile
            - {{ .Values.exporter.aws.profileName | quote }}
            {{- end }}
            {{- if .Values.exporter.aws.quotaLimitCheckIntervalSeconds }}
            - --limits-check-interval
            - {{ .Values.exporter.aws.quotaLimitCheckIntervalSeconds | quote }}
            {{- end }}
            {{- if .Values.exporter.aws.quotaCurrentValueCheckIntervalSeconds }}
            - --currents-check-interval
            - {{ .Values.exporter.aws.quotaCurrentValueCheckIntervalSeconds | quote }}
            {{- end }}
            {{- if .Values.exporter.aws.refreshResourcesIntervalSeconds }}
            - --reload-checks-interval
            - {{ .Values.exporter.aws.refreshResourcesIntervalSeconds | quote }}
            {{- end }}
            {{- if .Values.exporter.prometheus.metricsPrefix }}
            - --namespace
            - {{ .Values.exporter.prometheus.metricsPrefix | quote }}
            {{- end }}
            {{- if .Values.exporter.prometheus.enableDurationMetrics }}
            - --enable-duration-metrics
            {{- else }}
            - --disable-duration-metrics
            {{- end }}
            - {{ join "," .Values.exporter.aws.enabledChecks }}
          {{- if .Values.exporter.aws.credentialSecretName }}
          envFrom:
            - secretRef:
                name: {{ .Values.exporter.aws.credentialSecretName | quote }}
                optional: false
          {{- end }}
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /metrics
              port: metrics
          readinessProbe:
            httpGet:
              path: /metrics
              port: metrics
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
{{- end }}